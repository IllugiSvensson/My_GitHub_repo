#!/bin/bash
#Необходимые переменные и библиотека функций
appfolder=`realpath $0`
appfolder=${appfolder%/httpN_Linux}
. $appfolder/system/httpN_Linux_lib
cmdLine="$1"



#ЗАПУСК BIN ФАЙЛА
[ -e $appfolder/system/temp/Sessions/UPDATE ] && {	#Проверка на обновления

	zenity --warning --title=Предупреждение --text="Ведутся технические работы\nПопробуйте через минуту" --ellipsize --timeout=3
	exit

}
[ -z $cmdLine ] && {								#Вызывается если нет аргументов

	#Строим окно для ввода данных
	input=$(zenity --entry --title="httpN отладка" --width=256 --height=144 --text="Введите имя хоста" --entry-text="app hostname" --ok-label="Продолжить" --cancel-label="Выход")
	if [ $? == 0 ]		#Разбираем командную строку и запускаем подключение
	then

		cmdLine=httpn://${input:0:3}"%20"${input#${input:0:4}}

	else exit;
	fi

}



#ПРОВЕРКА ИМЕНИ И ПРАВ ПОЛЬЗОВАТЕЛЯ ПО СПИСКУ АВТОРИЗАЦИИ
[ `echo ${#username}` == 0 ] && {					#На случай, если не определилось имя

	message=`echo -e "🛑<b>Ошибка конфигурации</b>\n❌Имя компьютера не определено\n ⏱$(_Now)"`
	BotMsg $sBotKey $nChatId 0 "$message"
	Logger "Имя компьютера не определено. Проверьте компьютер пользователя." 2
	zenity --error --title=Ошибка --ellipsize --text="Имя компьютера не определено.\nОбратитесь в Отдел Тестирования." --timeout=3
	exit

}
autorizedUser="`FileReader $appfolder/system/USERS $username`"		#Ищем пользователя в списке
#Захватим название выбранного пользователем компьютера в виде кода стойки-стенда.
hostName=`echo ${cmdLine#${cmdLine:0:14}}`							#Выбираем имя компьютера
	[ `echo ${#autorizedUser}` == 0 ] && { #Если имя пользователя отсутствует в списке, заканчиваем работу

		message=`echo -e "👤<b>Неизвестный Пользователь</b>\n️⚠️Пытался подключиться к хосту\n🖥️$hostName ⏱$(_Now)"`
		BotMsg $sBotKey $nChatId 0 "$message"
		Logger "Неавторизованный вход" 1 "Неизвестный Пользователь(unknown)" "$username" "$hostName"
		zenity --error --title=Ошибка --ellipsize --text="Авторизация не пройдена.\nОбратитесь в Отдел Тестирования." --timeout=3
		exit

	}
#Проверим права на подключение. Если в строке не найдем имя компьютера или ADMIN, то выдаем ошибку.
	if [[ -z `echo $autorizedUser | grep "$hostName"` && -z `echo $autorizedUser | grep "ADMIN"` ]]
	then

		zenity --warning --title=Предупреждение --ellipsize --text="Недостаточно пользовательских прав\nна подключение к $hostName" --timeout=3
		exit

	fi



#ПОЛУЧЕНИЕ ИНФОРМАЦИИ О КОМПЬЮТЕРЕ СТЕНДА ИЗ КОМАНДНОЙ СТРОКИ ЗАПУСКА
namehost="`FileReader $appfolder/system/HOSTS $hostName`" 			#Получим строку с хостом и прочей инфой, если она есть
hn=`echo ${namehost%% *}`
#Ищем информацию о хосте из списка хостов
	if [[ -z "$namehost" || `echo ${#hn}` != `echo ${#hostName}` ]]	#Если имя не найдено
	then

		message=`echo -e "🛑<b>Ошибка конфигурации</b>\n️❌Имя хоста не найдено\n🖥️$hostName ⏱$(_Now)"`
		BotMsg $sBotKey $nChatId 0 "$message"
		Logger "Имя компьютера $hostName не найдено. Проверьте схему, список и строку запуска." 2
		zenity --error --title=Ошибка --ellipsize --text="Имя компьютера не найдено.\nОбратитесь в Отдел Тестирования." --timeout=3
		exit

	fi
#Разбираем строку адреса и маршрута, если такой есть
addcheck=`echo $namehost | egrep -o "[A]((\w+\.){3}\w+)"`			#Получаем шаблон днс адреса компьютера
address=`echo ${addcheck#A}`										#Получаем днс адрес компьютера
gwcheck=`echo $namehost | egrep -o "[G]((\w+\.){3}\w+)"`			#Получаем шаблон адреса шлюза
mskcheck=`echo $namehost | egrep -o "[M]((\w+\.){3}\w+)"`			#Получаем шаблон маски шлюза
	#Проверим правильность ip-адрессов на соответствие частным сетям ipv4
	#"^((10|192|127|169)\.){1}((25[0..5]|(2[0..4]\d|1{0,1}\d){0,1}\d)(\.?)){3}$" пропускает значащие нули
	if [ `Validator "$address" "((\w+\.){3}\w+)"` == 1 ]
	then

		message=`echo -e "🛑<b>Ошибка конфигурации</b>\n️❌Ошибка в списке хостов\n🖥️$hostName ⏱$(_Now)"`
		BotMsg $sBotKey $nChatId 0 "$message"
		Logger "В записи адреса $hostName ошибка. Проверьте запись в списке хостов." 2
		zenity --error --title=Ошибка --ellipsize --text="Ошибка в списке хостов.\nОбратитесь в Отдел Тестирования." --timeout=3
		exit

	fi
	if [ ! -z `echo $gwcheck` ]
	then

		flag=1								#Если маршрут есть, устанавливаем флаг для запуска с маршрутом
		gateway=`echo ${gwcheck#G}`
		maskAddr=`echo ${mskcheck#M}`
		gatemask=`AddrToMask $maskAddr`		#Получаем маску сети
		if [ `Validator "$gateway" "((\w+\.){3}\w+)"` == 1 ] || [ `Validator "$maskAddr" "((\w+\.){3}\w+)"` == 1 ]
		then

			message=`echo -e "🛑<b>Ошибка конфигурации</b>\n️❌Ошибка в списке хостов\n🖥️$hostName ⏱$(_Now)"`
			BotMsg $sBotKey $nChatId 0 "$message"
			Logger "В записи адреса $hostName ошибка. Проверьте запись в списке хостов." 2
			zenity --error --title=Ошибка --ellipsize --text="Ошибка в списке хостов.\nОбратитесь в Отдел Тестирования." --timeout=3
			exit

		fi

	else							#Если маршрута нет, устанавливаем флаг запуска напрямую

			flag=0					#Обнуляем параметры, чтобы программа не падала
			gateway=0
			maskAddr=0
			gatemask=0

	fi



#ЗАПУСК ПРИЛОЖЕНИЯ И НАБЛЮДЕНИЕ ЗА ХОДОМ РАБОТЫ
binFile=`echo ${cmdLine:0:11}`		#Выбираем из аргумента приложение для запуска
bins=`echo ${appfolder%/httpN}`
#Запускаем приложение с нужными параметрами
	case $binFile in
	"httpn://VNC")
		binFile="$bins/vnc/vncviewer"
		config=" -config $bins/vnc/config/"
		TrackBinFile "VNC" "$binFile" "$config" $flag ".vnc"
		;;
	"httpn://KIT")
		binFile="$bins/kitty/putty "
		config=" -load "
		pswrd=`cat $bins/kitty/Sessions/linux/$hostName | grep "pw *"`
		TrackBinFile "Putty" "$binFile" "$pswrd$config" $flag
		;;
	"httpn://SCP")
		binFile="$bins/winscp/filezilla"
		config=" -c 0/"
		TrackBinFile "FileZilla" "$binFile" "$config" $flag
		;;
	*)
		message=`echo -e "🛑<b>Ошибка конфигурации</b>\n️❌Ошибка в ссылке на схеме\n🖥️$hostName ⏱$(_Now)"`
		BotMsg $sBotKey $nChatId 0 "$message"
		Logger "При запуске $binFile произошла ошибка. Проверьте схему, записи и диск GetStand" 2
		zenity --error --title=Ошибка --ellipsize --text="Ошибка запуска приложения.\nОбратитесь в Отдел Тестирования." --timeout=3
		exit
		;;
	esac